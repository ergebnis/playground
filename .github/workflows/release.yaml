# https://docs.github.com/en/actions

name: "Release"

on: # yamllint disable-line rule:truthy
  push:
    tags:
      - "**"

jobs:
  release:
    name: "Release"
    uses: 'ergebnis/playground/.github/workflows/release-resuable.yaml@main'
    with:
      secrets:
        WORKFLOW_TOKEN: "${{ secrets.ERGEBNIS_BOT_TOKEN }}"
